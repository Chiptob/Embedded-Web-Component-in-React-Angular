# Embedded Web-Component in React and Angular Application

This small sample project shows the integration of a web component in Angular and React.

## Run

For Angular
Run `ng serve --open`

For React
Run `npm run start`
